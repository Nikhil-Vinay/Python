# take input from console and populate in list
# list1 = [int(i) for i in input().split(" ")] # split is string method

# initialize a list for n empty strings
# list = [""] * n

# python function can return multiple values together. Check out exaple: "return_multiple_val.py" here.
